apiVersion: v1
kind: ReplicationController
metadata:
   name: javaapp-rc
spec:
  replicas: 3
selector:
   app: myjavaapp
   version: 2.6.2
   metadata: 
   lables:
     app: myjavaapp
     version: 2.6.2
   spec:
     containers: 
     - name: myjavaapp-conatiner
       image: nginx
       ports:
       - containerPort: 8080
  

